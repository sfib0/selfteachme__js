// null represents the absence of a value.

var nullvalue = null;
var undef;
console.log(nullvalue);

// difference b/w null and undefined

// undefined don't accquire value in memory because it is not initialized.
// null accquire value in memory because it is initialized with a value of null.

// undefined is a type itself.
console.log(typeof undef);

// Type of a null value is object.
console.log(typeof nullvalue);

// undefined value is assigned to variables having only declaration.
// null can be assigned to represent variables having no value.